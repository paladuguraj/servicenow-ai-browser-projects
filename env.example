# ServiceNow credentials (copy to .env and fill in)
#   cp env.example .env
#
# For multiple instances, keep separate files and select with ENV_FILE:
#   cp env.example .env.adcom
#   ENV_FILE=.env.adcom npm run test:connection
SN_INSTANCE_URL=https://adcomsolutionsdev.service-now.com
SN_USERNAME=admin
SN_PASSWORD=your-password
